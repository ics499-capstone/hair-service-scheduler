language: python
python:
  - "3.8"
env:
  - FLASK_APP=flaskr
  - FLASK_ENV=development
  - SQLALCHEMY_DATABASE_URI=sqlite:///app.db
install:
  - pip install -r requirements.txt
  - pip install -e .
script:
  - pytest